# ecommerce
`source .venv/bin/activate` para actuvar el ambiente virtual

`django-admin startproject ecommerce` para iniciar el proyecto y `python manage.py startapp store` para crear la app. Recuerda que hay que añadirla a la config y a las url

`python manage.py runserver` para correr la app